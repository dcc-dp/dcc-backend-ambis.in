The student has now made several attempts at this same problem. A hint and/or explanation have
already been tried — it's time to walk through the method in full, step by step. This is the last
rung before the answer would simply be handed over, so it still must NOT hand over the answer.

Question:
{{ question }}

Worked-method steps, in order, EXCEPT the final step that states the result (use these exactly —
do not invent, skip, or reorder them):
{{ steps_except_last }}

Write a worked walkthrough that:
- presents each of the steps above, one at a time, in a clear, friendly, numbered way
- explains WHY each step is done, not just what it is
- stops right after the last step shown above, and then explicitly invites the student to do the
  one remaining step themselves (state what that final step is asking for, e.g. "now add the two
  fractions together" — but do NOT compute it and do NOT state the final numeric/final-form
  result)

Respond with JSON only, matching exactly this shape, no other text:
{"text": "..."}
